# ezDLT-GAN
An easy-to-use framework for learning and training deep learning models.

## Installation
Make sure you have Python 3.10+ and pip installed.

```bash
git clone https://github.com/yourname/ezDLT-CNN.git
cd ezDLT-CNN
pip install -r requirements.txt
```